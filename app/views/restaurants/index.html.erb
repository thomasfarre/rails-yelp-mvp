<div class="container">
  <% @restaurants.each do |restaurant| %>
    <restaurant><h2><%= link_to restaurant.name, restaurant_path(restaurant) %></h2>
    </restaurant>
  <% end %>
</div>

<%= link_to "New Restaurant", new_restaurant_path %>
